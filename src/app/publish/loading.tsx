'use client';

import { PublishPageSkeleton } from '@/components/skeleton';

/**
 * Publish Page Loading State
 * Displays editorial skeleton while publish console loads
 */
export default function PublishLoading() {
    return <PublishPageSkeleton />;
}
