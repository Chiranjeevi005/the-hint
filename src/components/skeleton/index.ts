/**
 * Editorial Skeleton Components
 * 
 * Barrel export for all skeleton loading placeholders.
 * These components exactly mirror the page layouts they represent.
 */

export {
    // Base primitives
    SkeletonBlock,

    // Page-level skeletons (used in loading.tsx files)
    HomepageSkeleton,
    SectionPageSkeleton,
    ArticlePageSkeleton,
    PublishPageSkeleton,
    ArticleDatabaseSkeleton,

    // Utility components
    SlowLoadingHint,
} from './EditorialSkeleton';
